meta:
  environment: node
  toolchain:
    name: npm
    version: "18"
build:
  artifacts:
    - .next/standalone
    - .next/static
    - public
  commands:
    - npm install
    - npx prisma generate
    - npm run build
run:
  command: npm run start
  persistenceMount: /data
  containerPort: 3000